import React from 'react'

const Button = () => {
  return (
    <div className="button-area">
        <button type="button">
            CLICK
        </button>
    </div>
  );
};

export default Button;
